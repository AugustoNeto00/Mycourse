# In this example, we define a function that calculates the area of a rectangle given its width and height.
def calcula_area(largura, altura):
    return largura * altura
print("Área do retângulo:", calcula_area(5, 10))