# Defines a function to check if a person is of legal age (18 or older)
def verifica_maioridade(idade):
    return idade >= 18

print(verifica_maioridade(20))  # True
print(verifica_maioridade(16))  # False